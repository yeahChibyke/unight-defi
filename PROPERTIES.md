# Properties

> Generated by the Fizz skill as a property inventory. Live settlement
> properties are exercised by the Base fork Foundry suites; deterministic
> accounting and math properties are exercised by `test/fizz/ToolFuzzTester.sol`.
>
> `[ ]` pending · `[x]` implemented · `[-]` skipped/manual. IDs are stable.
> `SHOULD-HOLD` is reserved for an exact accounting identity or an explicit
> contract guarantee; `EXPLORATORY` requires human review if violated.

## Global Properties

> Always-on checks after every reachable handler call.

- [ ] **GL-01** — `committedBuyerAssets` equals `autoLendBuyerAssets + bidBoardBuyerAssets`. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — exact update identity in `onBuy`; Priority: HIGH; Scope: always-on; Sources: CON-01, CON-03, SPEC-01)
- [ ] **GL-02** — `committedBuyerAssets` never exceeds `policy.globalCap`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Scope: always-on; Sources: CON-02, ADV-06, SPEC-03)
- [ ] **GL-03** — `autoLendBuyerAssets` never exceeds `policy.autoLendCap`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Scope: always-on; Sources: CON-04, ADV-06)
- [ ] **GL-04** — `bidBoardBuyerAssets` never exceeds `policy.bidBoardCap`. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Scope: always-on; Sources: CON-05, ADV-06)
- [ ] **GL-05** — `committedBuyerAssets`, `autoLendBuyerAssets`, `bidBoardBuyerAssets`, and `v4PrincipalRemoved` are monotonic. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — these counters have only unchecked-increment writes; Priority: HIGH; Scope: always-on; Sources: CON-06, ST-08, SPEC-04)
- [ ] **GL-06** — `v4PrincipalRemoved` is at least `committedBuyerAssets` after every successful settlement. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Scope: always-on; Sources: CON-07, SPEC-05)
- [ ] **GL-07** — A closed account reports zero `remainingCapacity()` and zero `remainingBidCapacity()`. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — both views explicitly return zero when `closed`; Priority: HIGH; Scope: always-on; Sources: CON-08, ST-12)
- [ ] **GL-08** — A disabled account reports zero `remainingCapacity()` and zero `remainingBidCapacity()`. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — both views explicitly return zero when `policy.enabled` is false; Priority: HIGH; Scope: always-on; Sources: CON-09, ST-12)
- [ ] **GL-09** — `policyNonce` and `positionEpoch` never decrease. (Category: VARIABLE_TRANSITION; Guarantee: SHOULD-HOLD — both are incremented only in policy/lifecycle transitions; Priority: MEDIUM; Scope: always-on; Sources: ST-01, ST-02, ST-03)
- [ ] **GL-10** — When the execution context is idle, its state is `Idle` and its committed fields are zero. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — successful callback cleanup uses `delete _execution`; Priority: HIGH; Scope: always-on; Sources: ST-09, CON-08)
- [ ] **GL-11** — The factory’s `accountOf(owner, positionId)` mapping is either unset or points to the deterministically predicted account. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `createAccount` stores the CREATE2 result and `predictAccount` uses the same salt/init code; Priority: HIGH; Scope: always-on; Sources: RT-04, ST-16)
- [ ] **GL-12** — A policy’s configured mode caps never exceed its global cap. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — `setPolicy` rejects either mode cap above `globalCap`; Priority: HIGH; Scope: always-on; Sources: CON-02, CON-04, CON-05, ADV-06)
- [ ] **GL-13** — No successful settlement increases a mode counter without increasing the global committed counter by the same gross buyer-assets amount. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — both writes use the same `buyerAssets` in `onBuy`; Priority: HIGH; Scope: always-on; Sources: CON-01, CON-03, ST-08)
- [ ] **GL-14** — No successful settlement leaves a nonzero loan-token allowance to Midnight beyond the exact most recent `buyerAssets` amount. (Category: HIGH_LEVEL; Guarantee: SHOULD-HOLD — `_approveExact` clears a prior allowance and approves exactly the requested amount; Priority: HIGH; Scope: always-on; Sources: CON-07, ADV-08, SPEC-06)

## Specific Properties

> Operation-gated postconditions. These require the named handler to snapshot
> state, perform one operation, and check the postcondition.

- [ ] **SP-01** — A successful `setPolicy` stores the supplied policy and increments `policyNonce` by one. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `setPolicy` writes `policy` and performs one unchecked increment; Priority: HIGH; Scope: after `unightAccount_setPolicy`; Sources: ST-01, ST-02)
- [ ] **SP-02** — A successful `disablePolicy` sets `policy.enabled` false and increments `policyNonce` by one. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit writes in `disablePolicy`; Priority: HIGH; Scope: after `unightAccount_disablePolicy`; Sources: ST-03, ADV-03)
- [ ] **SP-03** — A successful `setExecutor` changes only the selected executor permission and does not change policy, caps, epochs, or settlement counters. (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; Priority: MEDIUM; Scope: after `unightAccount_setExecutor`; Sources: ST-05, ADV-01)
- [ ] **SP-04** — A successful `close` sets `closed` and disables the policy, increments both epochs once, and revokes the account’s Midnight authorization when present. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit `close` postconditions; Priority: HIGH; Scope: after `unightAccount_close`; Sources: ST-04, ST-06, ADV-02)
- [ ] **SP-05** — `withdrawPosition` succeeds only after closure and zero selected-market credit/debt, transfers the configured NFT to the owner, and increments `positionEpoch`. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit `withdrawPosition` guards and writes; Priority: HIGH; Scope: after `unightAccount_withdrawPosition`; Sources: ST-07, ADV-13)
- [ ] **SP-06** — A successful factory creation emits an account whose immutable owner and position ID match the request, and a second creation for the same pair reverts. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — constructor arguments and `AccountExists` guard; Priority: HIGH; Scope: after `unightAccountFactory_createAccount`; Sources: RT-04, ST-16, ADV-04)
- [ ] **SP-07** — A successful `registerBidContext` leaves a MakerBid context containing the supplied offer hash, taker, max-assets bound, deadline, current epochs, and pre-settlement credit/debt. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `_execution` is initialized from those exact values; Priority: HIGH; Scope: after `unightAccount_registerBidContext`; Sources: ST-09, ADV-05, SPEC-07)
- [ ] **SP-08** — A successful `onBuy` increases global and mode-specific accounting by exactly `buyerAssets`, increases principal-removed accounting, approves exactly `buyerAssets`, and returns the context to idle. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — explicit postconditions in `onBuy`; Priority: HIGH; Scope: after `unightAccount_onBuy`; Sources: CON-01, CON-07, ST-08, ST-09, SPEC-01)
- [ ] **SP-09** — A failed `onBuy` caused by caller, market, callback-data, fee, health/debt, maturity/deadline, minimum-rate, capacity, or adapter validation leaves execution context, counters, balances, and allowance unchanged. (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; Priority: HIGH; Scope: after negative `unightAccount_onBuy` cases; Sources: ADV-10, ADV-11, ADV-12, ADV-14, ADV-15, ADV-16, ADV-17, SPEC-08)
- [ ] **SP-10** — A successful Auto-Lend take increases only `autoLendBuyerAssets` (and the global counter), preserves owner debt, and leaves the context idle. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — `takeAutoLend` requires unchanged debt and `onBuy` selects the Auto-Lend branch; Priority: HIGH; Scope: after `unightAccount_takeAutoLend`; Sources: CON-03, ST-10, SPEC-09)
- [ ] **SP-11** — Auto-Lend rejects maker callbacks, expired offers/policy, zero or under-minimum units, excessive assets, missing authorization, debt, and unapproved ratifiers without changing state. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — each rejection is an explicit precondition or revert path; Priority: HIGH; Scope: after negative `unightAccount_takeAutoLend` cases; Sources: ADV-05, ADV-07, ADV-09, ADV-10, ADV-12)
- [ ] **SP-12** — Bid Board settlement rejects stale policy/position epochs, wrong taker/buyer/market, malformed callback data, expired or post-maturity settlement, excessive fees, unhealthy/debt-bearing state, and insufficient units without changing state. (Category: VALID_STATE; Guarantee: SHOULD-HOLD — callback context and settlement guards explicitly reject these conditions; Priority: HIGH; Scope: after negative bid-board callback cases; Sources: ADV-10, ADV-11, ADV-12, ADV-14, ADV-15, ADV-16, ADV-17, SPEC-08)
- [ ] **SP-13** — Registry approval setters are callable only by the registry owner and each successful setter changes only its selected approval. (Category: STATE_TRANSITION; Guarantee: SHOULD-HOLD — all four setters use `onlyOwner` and write one mapping; Priority: HIGH; Scope: after registry setter handlers; Sources: ST-14, ADV-01, SPEC-10)
- [ ] **SP-14** — Repeated valid partial fills never make any global or mode counter exceed its configured cap, and each accepted fill is no greater than the capacity observed before that fill. (Category: HIGH_LEVEL; Guarantee: EXPLORATORY; Priority: HIGH; Scope: after successful Auto-Lend and Bid Board fills; Sources: CON-02, CON-04, CON-05, ADV-06, SPEC-03)
- [ ] **SP-15** — Offer-group consumption and cancellation/replay attempts cannot authorize a second settlement for units already consumed or invalidated. (Category: STATE_TRANSITION; Guarantee: EXPLORATORY; Priority: HIGH; Scope: after ratifier/take sequences; Sources: ADV-05, ADV-18, SPEC-11)
- [ ] **SP-16** — Conversion and capacity calculations are monotonic in requested units/assets, respect zero and sub-unit inputs, and never wrap narrow `uint128` policy or offer fields. (Category: VARIABLE_TRANSITION; Guarantee: EXPLORATORY; Priority: MEDIUM; Scope: after parameterized settlement/math sequences; Sources: RT-01, RT-02, RT-03, ADV-19, SPEC-12)
